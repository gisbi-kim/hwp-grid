diff --git a/src/model/table.rs b/src/model/table.rs
index f84da00..e731617 100644
--- a/src/model/table.rs
+++ b/src/model/table.rs
@@ -681,6 +681,41 @@ impl Table {
     ///
     /// 걸친 행 중 하나라도 `row_span==1` 선언이 없으면(미지 행) 기존 분배 규칙이
     /// 담당하므로 0 을 돌려준다. 반환 벡터의 길이는 `row_count` 다.
+    /// Trust a complete, internally consistent stored micro-grid. Empty cells in
+    /// a vertically subdivided row do not each require a full font-sized line.
+    pub fn coherent_stored_row_heights(&self) -> Option<Vec<HwpUnit>> {
+        let n = self.row_count as usize;
+        if n < 2 || !self.common.treat_as_char || self.cell_spacing != 0
+            || !self.local_resize_cols.is_empty() { return None; }
+        let mut rows = vec![0; n];
+        for cell in &self.cells {
+            let r = cell.row as usize;
+            if r >= n || cell.height == 0 || cell.height >= 0x80000000 { return None; }
+            if cell.row_span == 1 { rows[r] = rows[r].max(cell.height); }
+            for p in &cell.paragraphs {
+                if !p.controls.is_empty() { return None; }
+                if p.text.chars().any(|c| c > '\u{001f}') {
+                    if p.line_segs.is_empty() { return None; }
+                    for line in &p.line_segs {
+                        if line.tag & 0x80000000 != 0 || line.vertical_pos < 0
+                            || line.vertical_pos.saturating_add(line.line_height) as u32 > cell.height.saturating_add(2) { return None; }
+                    }
+                }
+            }
+        }
+        if rows.contains(&0) { return None; }
+        let sum: u64 = rows.iter().map(|&v| v as u64).sum();
+        if sum.abs_diff(self.common.height as u64) > 2 { return None; }
+        for cell in &self.cells {
+            let r = cell.row as usize;
+            let end = r + cell.row_span as usize;
+            if end > n || end <= r { return None; }
+            let span: u64 = rows[r..end].iter().map(|&v| v as u64).sum();
+            if span.abs_diff(cell.height as u64) > 2 { return None; }
+        }
+        Some(rows)
+    }
+
     pub fn rowspan_declared_overflow_shrink(&self) -> Vec<HwpUnit> {
         let row_count = self.row_count as usize;
         let mut shrink = vec![0 as HwpUnit; row_count];
diff --git a/src/renderer/height_measurer.rs b/src/renderer/height_measurer.rs
index 05cbef1..89d3792 100644
--- a/src/renderer/height_measurer.rs
+++ b/src/renderer/height_measurer.rs
@@ -2793,6 +2793,9 @@ impl HeightMeasurer {
             }
         }
 
+        if let Some(stored) = table.coherent_stored_row_heights() {
+            row_heights = stored.into_iter().map(|h| hwpunit_to_px(h as i32, self.dpi)).collect();
+        }
         // 셀 간격 포함한 표 높이
         let cell_spacing = hwpunit_to_px(table.cell_spacing as i32, self.dpi);
         let raw_table_height: f64 =
diff --git a/src/renderer/layout/table_layout.rs b/src/renderer/layout/table_layout.rs
index 775b47e..fc6f2c2 100644
--- a/src/renderer/layout/table_layout.rs
+++ b/src/renderer/layout/table_layout.rs
@@ -3574,6 +3574,9 @@ impl LayoutEngine {
                 });
             let measured = tail_fitted.as_ref().unwrap_or(mt);
             let mut rh = measured.row_heights.clone();
+            if let Some(stored) = table.coherent_stored_row_heights() {
+                return stored.into_iter().map(|h| hwpunit_to_px(h as i32, self.dpi)).collect();
+            }
             rh.resize(row_count, hwpunit_to_px(400, self.dpi));
             // [#3386] 행별 저장-선언 신뢰(렌더 전용): 전 셀이 rowspan 없이 저장
             // LINE_SEG 를 보유하고 행별 선언(cellSz)이 모두 존재하며, 선언 합이
@@ -3589,6 +3592,9 @@ impl LayoutEngine {
         }
 
         // 1단계: row_span==1인 셀에서 개별 행 높이 추출
+        if let Some(stored) = table.coherent_stored_row_heights() {
+            return stored.into_iter().map(|h| hwpunit_to_px(h as i32, self.dpi)).collect();
+        }
         let mut row_heights = vec![0.0f64; row_count];
         // 행별 **컨텐츠** 하한 — 2단계 축소 규칙(#5910)의 바닥. HeightMeasurer 미러.
         let mut content_row_floor = vec![0.0f64; row_count];
diff --git a/src/renderer/typeset.rs b/src/renderer/typeset.rs
index c60ea9e..3698d6a 100644
--- a/src/renderer/typeset.rs
+++ b/src/renderer/typeset.rs
@@ -3467,6 +3467,24 @@ fn para_has_floating_object(para: &Paragraph) -> bool {
 ///
 /// 오탐을 막기 위해 두 문단이 같은 단 기하(`column_start`/`segment_width`)를 쓰고,
 /// 어울림 개체가 없으며, 양쪽 LINE_SEG 가 합성본이 아닌 경우로만 좁힌다.
+fn saved_native_reset(prev: &LineSeg, curr: &LineSeg) -> bool {
+    let real = |l: &LineSeg| !is_synthetic_line_seg(l) && l.tag & LineSeg::TAG_FIRST_SEGMENT != 0 && l.line_height > 0 && l.segment_width > 0;
+    real(prev) && real(curr) && curr.vertical_pos >= 0 && curr.vertical_pos < 5000 && prev.vertical_pos > curr.vertical_pos + 1000
+}
+fn has_side_wrap(para: &Paragraph) -> bool {
+    para.controls.iter().any(|c| {
+        let common = match c {
+            Control::Table(t) => &t.common,
+            Control::Picture(p) => &p.common,
+            Control::Shape(s) => s.common(),
+            Control::Equation(e) => &e.common,
+            _ => return false,
+        };
+        !common.treat_as_char && matches!(common.text_wrap,
+            crate::model::shape::TextWrap::Square | crate::model::shape::TextWrap::Tight | crate::model::shape::TextWrap::Through)
+    })
+}
+
 fn stored_vpos_top_collision(prev: &Paragraph, curr: &Paragraph) -> bool {
     if para_has_floating_object(prev) || para_has_floating_object(curr) {
         return false;
@@ -7478,6 +7496,11 @@ impl TypesetEngine {
             // sample16-2022 pi=87 (빈 문단, text_len=0) skip ✓
             // sample16-2022 pi=118 (content, sb=284) skip ✓
             // sample16-2022 pi=316 (content, sb=0) skip ✓
+            let native_saved_reset = profile.hwp5_stored_pagination_layout()
+                && st.col_count == 1 && para_idx > 0
+                && !has_side_wrap(para) && !has_side_wrap(&paragraphs[para_idx - 1])
+                && paragraphs[para_idx - 1].line_segs.last().zip(para.line_segs.first())
+                    .is_some_and(|(a,b)| saved_native_reset(a,b));
             let variant_vpos_reset_break = profile.hwp3_layout()
                 && self.judge_hwp3_variant_vpos_reset_break(
                     para,
@@ -7504,7 +7527,7 @@ impl TypesetEngine {
                 st.behind_float_table_para = None;
             }
 
-            if (force_page_break || para_style_break || variant_vpos_reset_break)
+            if (force_page_break || para_style_break || variant_vpos_reset_break || native_saved_reset)
                 && !st.current_items.is_empty()
             {
                 st.force_new_page();
@@ -17146,6 +17169,10 @@ impl TypesetEngine {
         let hwp3_converted_hwp5 = st.profile.hwp3_layout()
             && !st.profile.hwp3_native_layout()
             && !st.profile.hwpx_container();
+        let native_internal_reset = (st.profile.hwp5_stored_pagination_layout()
+            && st.col_count == 1 && !has_side_wrap(para)).then(|| {
+                para.line_segs.windows(2).position(|pair| saved_native_reset(&pair[0], &pair[1])).map(|i| i + 1)
+            }).flatten();
         let internal_forced_page_break_line = internal_vpos_page_break_line(
             para,
             fmt.line_heights.len(),
@@ -17170,7 +17197,7 @@ impl TypesetEngine {
                     self.dpi,
                 )
         });
-        let forced_page_break_line = internal_forced_page_break_line
+        let forced_page_break_line = native_internal_reset.or(internal_forced_page_break_line)
             .or_else(|| {
                 st.profile.hwpx_stored_layout().then(|| {
                     hwpx_explicit_page_break_tail_line(
@@ -21092,7 +21119,22 @@ impl TypesetEngine {
         // 경계는 전부 reset 기록이 있는 표였고(384쪽 계약 보존), hwpctl 원장 표는
         // 기록이 없다 — RHWP_DIAG_6307 실측.
         let table_storage_declares_splits = rowbreak_table_has_internal_saved_vpos_reset(table);
+        let saved_first_row_boundary = if st.profile.hwp5_stored_pagination_layout()
+            && !is_continuation && cursor_row == 0 && !table.common.treat_as_char
+            && matches!(table.page_break, crate::model::table::TablePageBreak::RowBreak)
+            && matches!(table.common.text_wrap, crate::model::shape::TextWrap::TopAndBottom)
+            && table.common.height > 0 {
+                let mut sum = 0u64;
+                (0..row_count).find_map(|row| {
+                    let height = table.cells.iter().filter(|c| c.row as usize == row && c.row_span == 1).map(|c| c.height).max().unwrap_or(0);
+                    sum += height as u64;
+                    (row + 1 < row_count && height > 0 && sum.abs_diff(table.common.height as u64) <= 2
+                        && table.cells.iter().all(|c| c.row as usize >= row + 1 || c.row as usize + c.row_span as usize <= row + 1))
+                        .then_some(row + 1)
+                })
+            } else { None };
         while r < row_count {
+            if saved_first_row_boundary == Some(r) { break; }
             let cs_before = if r > cursor_row { cs } else { 0.0 };
             // [#3820 Stage 76] 직전 fragment가 내용은 모두 소비한 채 남긴
             // rowspan 행의 빈 꼬리 밴드. start_cut이 셀 텍스트를 숨기므로 여기서는
@@ -21148,8 +21190,11 @@ impl TypesetEngine {
             // 행 시작 y offset을 빼서 아래 행 셀을 다음 조각에 남긴다.
             // #1105처럼 hard-break가 뒤 행 셀 안에 있는 블록은 기존 블록 컷을
             // 유지해야 첫 조각의 `end_cut`이 한컴 기준과 맞는다.
-            let rowbreak_use_row_offsets =
-                rowbreak_rowspan_block && rowbreak_hard_break_row == Some(b_start);
+            let nested_stacked_rows = mt.allows_row_break_split() && block_has_any_rowspan
+                && table.cells.iter().any(|cell| cell.row as usize == b_start && cell.row_span == 1
+                    && cell.paragraphs.iter().any(|p| p.controls.iter().any(|c| matches!(c, Control::Table(_)))));
+            let rowbreak_use_row_offsets = nested_stacked_rows
+                || (rowbreak_rowspan_block && rowbreak_hard_break_row == Some(b_start));
             if (protected || rowbreak_rowspan_block) && b_start == r {
                 // [Task #1025] 연속분 커서가 블록 중간이면 블록 시작 컷을 적용.
                 let blk_start_cut: &[usize] = if r == cursor_row { &start_cut } else { &[] };

